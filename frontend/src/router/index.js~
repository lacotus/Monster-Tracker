import Vue from 'vue'
import Router from 'vue-router'
import Register from '@/components/Register.vue'

Vue.use(Router)

const newRouter = new Router({
  routes: [
    {
      path: '/register',
      name: 'register',
      component: Register
    }
  ]
})

export newRouter

